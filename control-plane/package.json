{
  "name": "diffract-control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Diffract multi-tenant control plane: Dodo payment webhook -> Hostinger VPS provisioner -> per-client *.diffraction.in ingress.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test"
  }
}
